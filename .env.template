# DataGate Environment Configuration
# Railway.appで設定する環境変数のテンプレート

# 基本設定
NODE_ENV=production
PORT=3000

# アプリケーションURL
BASE_URL=https://datagate-poc-production.up.railway.app

# SMTP設定（将来実装用）
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# セキュリティ設定
SECURE_LINK_EXPIRY_DAYS=7
MAX_FILE_SIZE_MB=10

# 通知メール設定
NOTIFICATION_FROM=noreply@datagate.com
NOTIFICATION_SUBJECT_PREFIX=[DataGate]

# ストレージ設定
UPLOAD_DIR=uploads
TEMP_DIR=temp
